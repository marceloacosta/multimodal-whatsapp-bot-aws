# =============================================================================
# ENVIRONMENT VARIABLES REFERENCE
# =============================================================================
# 
# This file is a REFERENCE ONLY. You do NOT need to create a .env file.
# 
# In AWS, configuration is handled differently:
# 
# 1. WhatsApp credentials → AWS Secrets Manager (secret: "whatsapp-credentials")
# 2. Lambda env vars → Set in AWS Console or via CLI per-function
# 3. No .env file needed for deployment
#
# See ARCHITECTURE_DECISIONS.md for detailed explanation.
# =============================================================================

# WhatsApp Business API Configuration
# These go in AWS Secrets Manager (NOT Lambda environment variables)
# Get these from Meta for Developers: https://developers.facebook.com/
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_ACCESS_TOKEN=your_access_token_here
WHATSAPP_VERIFY_TOKEN=your_custom_verify_token_here

# AWS Configuration
# These are used in scripts and IAM policies
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your_aws_account_id_here

# =============================================================================
# Lambda Environment Variables (Set per-function in AWS Console or CLI)
# =============================================================================

# --- wa-process Lambda ---
BEDROCK_AGENT_ID=your_agent_id_here
BEDROCK_AGENT_ALIAS_ID=your_agent_alias_id_here
BEDROCK_REGION=us-east-1
MEDIA_BUCKET=your-media-bucket-name
WA_SEND_FUNCTION=wa-send
WA_TTS_FUNCTION=wa-tts

# --- wa-image-generate Lambda ---
IMAGE_BUCKET=your-generated-images-bucket-name
IMAGE_MODEL_ID=amazon.titan-image-generator-v2:0
IMAGE_PREFIX=generated-images/
WA_SEND_FUNCTION=wa-send

# --- wa-image-analyze Lambda ---
VISION_MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0

# --- inbound-webhook Lambda ---
MEDIA_BUCKET=your-media-bucket-name
WA_PROCESS_FUNCTION=wa-process

# --- Other Lambda Functions ---
# (Most other functions get config from Secrets Manager at runtime)

